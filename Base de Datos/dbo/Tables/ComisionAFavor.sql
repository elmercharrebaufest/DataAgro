CREATE TABLE [dbo].[ComisionAFavor] (
    [Id] INT          IDENTITY (1, 1) NOT NULL,
    [Descripcion]      VARCHAR (50) NOT NULL,
    CONSTRAINT [PK_ComisionAFavor] PRIMARY KEY CLUSTERED ([Id] ASC)
);

